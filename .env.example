# Frontend Environment Variables

# Supabase Configuration (Primary Backend)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Express Backend URL (for AI endpoints only)
VITE_API_URL=http://localhost:3001/api

# SECURITY NOTE: 
# - Gemini API key is server-side only (see server/.env.example)
# - Never expose API keys in VITE_ environment variables
# - VITE_ variables are embedded in the client bundle and publicly visible

# Analytics (optional)
VITE_GA_TRACKING_ID=
